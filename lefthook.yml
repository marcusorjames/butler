pre-commit:
  commands:
    shfmt:
      run: shfmt -w -i 2 butler bin/ scripts/
      stage_fixed: true
    shellcheck:
      run: find bin scripts -maxdepth 2 -type f | xargs shellcheck -x --severity=warning butler
    markdownlint:
      glob: "*.md"
      run: markdownlint-cli2 {staged_files}
