#!/bin/sh
# Docker compose passthrough

PACKAGE="butler docker-compose"

print_usage() {
  echo "Docker Compose passthrough"
  echo " "
  echo "SYNOPSIS:"
  echo " "
  echo "$PACKAGE COMMAND... [options]"
  echo ""
  echo "COMMAND / Options:"
  echo "See --help inherited from docker-compose"
}

[ ! "$#" -gt 0 ] && print_usage && exit 1

cmd="$1"
shift

# Ensure we are not in a docker directory as this breaks --project-directory
cd "$BIN_DIR" || exit

# Ensure nginx-proxy is up
docker ps | grep -q nginx-proxy || docker compose \
  --project-directory "$ROOT_DIR/common/nginx-proxy" up -d

# Run any site scripts
if [ -f "$CURRENT_SITE_DIR/scripts/$cmd" ]; then
  # shellcheck source=/dev/null
  . "$CURRENT_SITE_DIR/scripts/$cmd"
fi

docker compose --project-directory "$CURRENT_SITE_DIR" "$cmd" "$@"
